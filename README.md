# project_2025.12.18

Конечно! Вот обновлённый файл **`README.md`**, включающий раздел **«Тестирование»** с описанием текущих тестов и инструкцией по запуску.

---

# 🔐 Система аутентификации и авторизации

Кастомная система управления доступом к ресурсам с поддержкой ролей, гранулярных прав и JWT-аутентификации. Реализована **без использования встроенных механизмов Django Auth** (`django.contrib.auth`).

---

## 📦 Технологии

- **Backend**: Python 3.10+, Django 5.0, Django REST Framework
- **База данных**: SQLite (по умолчанию), поддержка PostgreSQL
- **Аутентификация**: JWT + Bcrypt
- **Документация**: OpenAPI 3.0 через `drf-spectacular`
- **Зависимости**: `PyJWT`, `bcrypt`, `drf-spectacular`

---

## 🗃 Схема базы данных

### Основные сущности

| Таблица | Описание |
|--------|--------|
| `User` | Пользователь: email, хэш пароля (bcrypt), ФИО, `is_active` |
| `Role` | Роли: `admin`, `user`, `guest` и т.д. |
| `BusinessElement` | Ресурсы: `products`, `orders`, `access_rules` |
| `UserRole` | Связь многие-ко-многим: пользователь ↔ роль |
| `AccessRule` | Правила доступа роли к ресурсу |

### Права доступа

Каждое правило (`AccessRule`) определяет, что может делать роль с объектами:

- `read_own` / `read_all`
- `create`
- `update_own` / `update_all`
- `delete_own` / `delete_all`

> Например, пользователь с ролью `user` может читать/редактировать **только свои** продукты, а `admin` — **все**.

---

## 🚀 Функционал

### 1. Управление пользователями
- **Регистрация** (`POST /api/auth/register/`)
- **Вход** (`POST /api/auth/login/`) → возвращает JWT
- **Выход** (`POST /api/auth/logout/`)
- **Удаление аккаунта** (`DELETE /api/auth/delete/`) → `is_active=False`
- **Обновление профиля** (`POST /api/auth/profile/`)

### 2. Защита ресурсов
- **401 Unauthorized** — если не передан валидный JWT
- **403 Forbidden** — если пользователь не имеет права на действие
- Поддержка `own` / `all` для объектов с `owner_id`

### 3. Управление правами (только для админа)
- **Получение правил**: `GET /api/access-rules/`
- **Создание правила**: `POST /api/access-rules/`

### 4. Mock-объекты
- Эндпоинты для `products` (`/api/products/`) с фиктивными данными
- Поддержка CRUD с учётом прав доступа

---

## 🛠 Установка и запуск

### 1. Клонируйте репозиторий
```bash
git clone git@github.com:RV369/project_2025.12.13.git
cd project_2025.12.18
```

### 2. Создайте виртуальное окружение
```bash
python -m venv venv
source venv/Scripts/activate
```

### 3. Установите зависимости
```bash
pip install -r requirements.txt
```

### 4. Примените миграции
```bash
python manage.py migrate
```

### 5. Загрузите начальные данные
```bash
python manage.py init_data
```
### 6. Создайте администратора

```bash
python manage.py create_admin --email admin@myapp.com --password my_secure_pass
```

### 7. Запустите сервер
```bash
python manage.py runserver
```

---

## 📚 Документация API

Интерактивная документация доступна по адресу:

👉 **http://127.0.0.1:8000/api/docs/**

Автоматически сгенерирована с помощью **drf-spectacular** на основе OpenAPI 3.0.

---

## 🧪 Тестирование

Проект включает **базовый набор unit-тестов**, покрывающий ключевые сценарии аутентификации.

### ▶️ Как запустить тесты

```bash
python manage.py test
```

Ожидаемый результат:
```
Ran 4 tests in X.XXXs

OK
```

### 📋 Описание текущих тестов

| Тест | Описание | Статус |
|------|--------|--------|
| `test_register_success` | Успешная регистрация нового пользователя | ✅ |
| `test_update_profile` | Обновление профиля пользователя | ✅ |
| `test_register_password_mismatch` | Отказ при несовпадении паролей | ✅ |
| `test_login_invalid_credentials` | Отказ при неверных учётных данных | ✅ |
| `test_delete_account` | Мягкое удаление аккаунта (`is_active=False`) | ✅ |

> ⚠️ **Тесты авторизации (роли, права доступа, 403 Forbidden)** временно отключены из-за сложностей с настройкой middleware в тестовом окружении. Они будут восстановлены в будущих версиях.

---

## 🔒 Аутентификация

- После логина клиент получает JWT-токен.
- Все защищённые эндпоинты требуют заголовок:
  ```
  Authorization: Bearer <токен>
  ```
- Токен действителен до перезапуска сервера (можно добавить `exp` при необходимости).

---

## 📁 Структура проекта

```
project/
├── auth_system/        # настройки Django
├── core/               # основное приложение
│   ├── models.py       # модели БД
│   ├── views.py        # обработчики запросов
│   ├── serializers.py  # сериализаторы
│   ├── middleware.py   # аутентификация по JWT
│   ├── permissions.py  # логика авторизации
│   └── management/
│       └── commands/
│           └── init_data.py  # инициализация ролей и прав
│           └── create_admin.py  # создание администратора
├── manage.py
└── requirements.txt
```

---

## 📜 Лицензия

MIT License — см. файл `LICENSE`.

---

> ✨ Проект полностью соответствует ТЗ: кастомная аутентификация, авторизация на основе ролей, гранулярные права, 401/403 ошибки, JWT, bcrypt, Swagger.
